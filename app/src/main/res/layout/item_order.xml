<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="?attr/selectableItemBackground">

    <TextView
        android:id="@+id/productName"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnBackground"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"/>

    <TextView
        android:id="@+id/quantity"
        android:textSize="14sp"
        android:textColor="?attr/colorOnBackground"  android:layout_height="wrap_content" android:layout_width="wrap_content"/>

    <TextView
        android:id="@+id/price"
        android:textSize="14sp"
        android:textColor="?attr/colorOnBackground"  android:layout_height="wrap_content" android:layout_width="wrap_content"/>

    <TextView
        android:id="@+id/orderDate"
        android:textSize="12sp"
        android:textColor="?attr/colorSecondary"  android:layout_height="wrap_content" android:layout_width="wrap_content"/>
</LinearLayout>
